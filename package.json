// package.json
{
  "name": "setup-renode",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
